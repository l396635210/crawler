{% extends 'base.html.twig' %}

{% block body %}
    <h1>Category creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class="btn btn-primary" style="margin: 10px;" type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a class="btn btn-link" href="{{ path('admin_report_category_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
