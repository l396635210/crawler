{% extends 'admin.html.twig' %}

{% block body %}
    <h2 class="sub-header">网站添加</h2>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <div class="form-group">
            <input  class="btn btn-success pull-right" type="submit" value="Create" />
        </div>
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('qa_site_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
